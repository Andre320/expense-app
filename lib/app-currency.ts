/** Reporting currency for ledger, dashboard, and forecasts. */
export const REPORTING_CURRENCY = "CRC" as const;

/** Secondary currency shown alongside CRC on transactions. */
export const QUOTE_CURRENCY = "USD" as const;
